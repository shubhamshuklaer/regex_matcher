1. Overview: A regex parser, it converts the given regex into a nfa then converts it into a dfa then finally to minimized dfa. The minimized dfa is the used to test any input string.
1. Compilation: make
2. Run: python start.py
3. Requirements: python 2.7,pyuic5,PyQt5(compiled with python 2.7), python-networkx, python-matplotlib, python-numpy, python-pygraphviz
4. Regex operators supported : 
    1. klene closure "*"
    2. one or more operator "?" 
    3. Or operator "+"
    4. concatination
    5. brackets
